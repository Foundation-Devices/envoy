## SPDX-FileCopyrightText: 2022 Foundation Devices Inc.
##
## SPDX-License-Identifier: GPL-3.0-or-later
#
#name: BeefQA
#
#on:
#  workflow_run:
#    workflows: [ "Check" ]
#    types:
#      - completed
#
#jobs:
#  beef-qa:
#    if: ${{ github.event.workflow_run.conclusion == 'success' }}
#    runs-on: [ self-hosted, macOS, ARM64 ]
#
#    steps:
#      - name: Extract PR number
#        id: extract_pr
#        run: |
#          echo "pr_number=$(jq -r '.pull_requests[0].number' <<< '${{ toJson(github.event.workflow_run) }}')" >> $GITHUB_OUTPUT
#
#      - name: Checkout PR code
#        uses: actions/checkout@v4
#        with:
#          ref: refs/pull/${{ steps.extract_pr.outputs.pr_number }}/merge
#          fetch-depth: 0
#
#      - uses: extractions/setup-just@v1
#
#      - name: Install bindgen-cli (for aws-lc-sys)
#        run: cargo install --force --locked bindgen-cli
#
#      - name: Build ffi
#        run: ./scripts/build_ffi_ios.sh
#
#      - name: Run BeefQA Tests
#        run: just qa
